# Environment variables
.env
.env.*
!.env.example

# 실제 Supabase/뉴스 API 키가 담기는 생성 파일 (scripts/generate_config.py 또는
# scripts/generate-config.mjs가 .env/환경변수로부터 재생성한다, 2026-07-14 common 병합 결정).
# 이 저장소는 public이라, 실제 키가 커밋 이력에 남지 않도록 로컬/빌드 전용으로 둔다.
/js/config.js

# Persona files
/persona/
/personas/

# OpenAPI spec files
openapi (1).json
openapi.json

# Python backend
__pycache__/
*.pyc
.venv/
venv/

# Wanted API fetch 캐시(import_wanted_data.py) — 공고 URL의 client_id 쿼리파라미터에 실제
# WANTED_API 자격증명 값이 그대로 포함되어 있어 커밋하지 않는다. 재fetch로 언제든 재생성 가능.
backend/scripts/data/

# scripts/import_wanted_data.py 중간 산출물(원티드 API 실호출 결과, 재실행마다 달라짐)
/scripts/data/

# Personal local Claude Code agent team config (not shared with team)
/.claude/
.vercel
.env*
